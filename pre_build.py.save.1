print("Pre build invoked")

mypath="."

from os import listdir
from os.path import isfile, join
import subprocess
import os

files  = [f for f in listdir(mypath) if isfile(join(mypath, f))]

for file in files:
	if ".png" in file:
		print("Processing: " + file)
                cmd = "convert " +  file + " rgba:" + file[:file.index(".png")] + ".raw"
		os.system(cmd)

#subprocess.run(["convert", file, " rgb
a:" + file[:file.index(".png")] + ".raw"])

#convert firefox.png rgba:firefox.raw

